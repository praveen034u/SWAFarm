# SWAFarm — TODO / Milestone Tracker

## Node V1 Hardware

### Power Supply Subsystem
- [x] Architecture designed (logical, full solar+battery scope) — 2026-07-29. See [`Documentation/SWAFarmNodeV1_PowerSupply_Design.md`](Documentation/SWAFarmNodeV1_PowerSupply_Design.md) and [`BOM/SWAFarmNodeV1_PowerSupply_BOM.csv`](BOM/SWAFarmNodeV1_PowerSupply_BOM.csv).
- [x] `01_power` schematic captured — 2026-07-30, narrower scope than the full architecture above: 12V DC input, fuse + TVS + low-side N-MOSFET reverse-polarity protection, LT8610AC buck to 3.3V (feedback divider, RT, EN/UV UVLO divider, soft-start, BST/INTVCC decoupling), power-good LED, 4 test points (VIN_PROT, 3V3_RAIL, GND, PG), net labels. Solar input, battery charging (BQ25792), and battery protection (BQ76920) from the architecture doc are **not yet captured in KiCad** — deferred to a follow-on milestone.
- [x] Design review package generated for `01_power` — 2026-07-30. See [`Documentation/SWAFarmNodeV1_01Power_DesignReview.md`](Documentation/SWAFarmNodeV1_01Power_DesignReview.md). The as-built BOM was merged into the single project BOM at [`BOM/SWAFarmNodeV1_PowerSupply_BOM.csv`](BOM/SWAFarmNodeV1_PowerSupply_BOM.csv) (added a Status column: Captured / Planned / Superseded) rather than kept as a separate file, to avoid two BOMs drifting out of sync.
- [x] **Wiring defects found and fixed — 2026-07-30.** Review of the netlist ahead of expert sign-off found the schematic was not actually ERC-clean (the committed `SWAFarmNodeV1_erc.json` snapshot at 21:52:27 was stale). Four defects were found and corrected directly in the `.kicad_sch` (manual S-expression edit, same technique as capture — see Tooling section):
  1. **U1's SW switching node was shorted to GND** by a stray `power:GND` symbol (`#PWR12`) placed directly on the SW/L1/C_BST1 wire — the most serious defect, not flagged by ERC by default (output-to-power-net isn't in the default severity matrix) and found only by netlist inspection. This was also the true cause of the ERC "Output/Power-output pin conflict (U1, #FLG01)" error — both `#FLG01` and `#FLG02` PWR_FLAGs were legitimate (on GND and VIN_PROT respectively, standard practice) and needed no change.
  2. R_gate1 (Q1's gate resistor) was sitting unwired on top of a through-running VIN_PROT→Q1-gate wire instead of being spliced into it. Fixed by splitting that wire around R_gate1's pins.
  3. D_LED1's cathode and anode both landed on the same SW-node wire (zero volts across it, would never light) — resolved as a side effect of fixing (1), but required relocating D_LED1 and adding a dedicated GND return since its old position was on the now-isolated SW net, not true ground.
  4. R_LED1 turned out to already be in series with D_LED1 via the wiring (the original review's claim that it wasn't was itself corrected during this fix).
  Live re-run: **ERC passes clean, 0 errors / 0 warnings.** Fresh `SWAFarmNodeV1_erc.json` exported 2026-07-30T23:56:59. Full writeup in [`Documentation/SWAFarmNodeV1_01Power_DesignReview.md`](Documentation/SWAFarmNodeV1_01Power_DesignReview.md).
- [x] **`08_BatterySolar` schematic captured — 2026-07-31.** Solar input port (`J2`/`F2`/`D2`/`Q2`/`R_gate2`) mirroring `01_power`'s DC-IN protection topology exactly. Battery charger `U_CHG1` = **BQ25798** (real stock KiCad symbol, VQFN-29) substituting the architecture doc's BQ25792 — no official symbol exists for BQ25792, same class of gap as the LT8609A→LT8610AC substitution, same-family/same-core-architecture part confirmed via full pin-table extraction (not guessed). Battery protection AFE `U_PROT1` = **BQ76920PW** (exact match, no substitution needed) with an independent low-side back-to-back FET pair (`Q_DSG1`/`Q_CHG1`), precision sense shunt (`R_SENSE1`), and a tertiary series fuse (`F3`) — a second hardware protection layer independent of the charger IC, per SWA-PWM-003. 4S LiFePO4 pack connector (`J3`) with full cell-tap sense network (VC0–VC4, VC5 tied to VC4 for 4S-on-5S-capable-device operation). Shared I2C bus (reusing `02_MCU`'s reserved PB8/PB9/PC4/PC5) plus a discrete low-IQ (~17µA) battery-voltage ADC divider on spare pin PB1 — both consumed for the first time this milestone. **`01_power`'s buck converter (U1) input was redirected from `VIN_PROT` to the new `VSYS` net** (charger output) — a disclosed, necessary, user-approved integration change to already-approved circuitry, not a silent modification; without it the new charger/battery stage would be electrically orphaned. Missing-requirement gate (battery capacity, solar panel spec, environmental temperature range — none specified anywhere in `.ai/knowledge`) resolved with the user before implementation: 3.35Ah/cell provisional capacity, 10W/≤20V Voc solar target, -20°C to +60°C operating range. Full current-consumption/battery-life/solar-sizing calculations, protection strategy, and classified ERC results in [`Documentation/SWAFarmNodeV1_08BatterySolar_DesignReview.md`](Documentation/SWAFarmNodeV1_08BatterySolar_DesignReview.md). Live ERC: 92 violations (45 errors / 47 warnings) — **2 fewer errors and zero new warnings** vs. the pre-milestone baseline, independently confirmed via `generate_netlist`/`trace_netlist_connection` on the charger, protection AFE, and battery pack connector.
- [ ] Confirm solar panel Voc spec against the charger's 22V VINDPM ceiling once a real panel is sourced (Risk R-1; user-approved planning target is 10W/≤20V Voc, not yet a purchased part)
- [ ] Second-source qualification for LT8610AC, BQ25798 (substitutes BQ25792), BQ76920PW (Risk R-2/R-BATT-6, SWA-CST-004)
- [ ] Formal power budget (SWA-PWM-201) to lock battery capacity (Risk R-3/R-BATT-4) — `08_BatterySolar`'s current-consumption table and illustrative-duty-cycle runtime estimate are a planning-level step toward this, not a substitute for it; also confirm F1's 1.1A PTC sizing against real load current
- [ ] Start LiFePO4 pack UN38.3 / IEC 62133 certification track (Risk R-4/R-BATT-7)
- [ ] Define surge test level (IEC 61000-4-5 class) and finalize GDT/TVS coordination (Risk R-5) — now applies to both DC-IN and Solar-IN ports
- [ ] Review the charger/AFE external application circuit (input-selector FETs, bootstrap network, ILIM/PROG resistor values, cell-tap filter network) against a locked BQ25798/BQ76920 datasheet before fabrication — captured per standard application-circuit convention this milestone, not pin-by-pin datasheet-verified (Risk R-BATT-1, highest-priority open item from `08_BatterySolar`)
- [ ] Footprint placement and routing in KiCad GUI (MCP tooling has no placement/routing capability — see Tooling note below) — handoff to schematic_designer/pcb_layout_reviewer roles
- [ ] Schematic review (pcb_layout_reviewer role) — reverse-polarity MOSFET part number, EN/UV UVLO threshold, and RT switching-frequency choice should be reconfirmed against the LT8610AC datasheet before release

### MCU Foundation Subsystem
- [x] `02_MCU` schematic captured — 2026-07-31. Discrete STM32L462RETx (LQFP64, AES-256/TRNG crypto variant — chosen over plain STM32L452 for SWA-SEC hardware-crypto requirement, no footprint/pinout cost) per `swafarm_overview.md`'s explicit discrete-MCU-over-integrated-module architecture recommendation. Includes: power decoupling (VDD/VDDA/VBAT/VDDUSB), reset network, BOOT0 config, LSE 32.768kHz RTC crystal, ARM 10-pin SWD programming header, USART1 debug console UART header, GPIO-driven status LED, NRST/BOOT0 test points. Ties into the existing `3V3_RAIL`/`GND` nets from `01_power` — no changes made to Power-subsystem circuitry. 27 GPIOs reserved (named via netlist, not wired) for LoRaWAN module, RS485, relay driver, sensor front-end, battery-monitor I2C, expansion SPI, and user buttons; 16 GPIOs left spare. Full writeup, GPIO allocation table, and classified ERC results in [`Documentation/SWAFarmNodeV1_02MCU_DesignReview.md`](Documentation/SWAFarmNodeV1_02MCU_DesignReview.md).
- [ ] Approve wiring Power subsystem's existing `PG` signal to a spare MCU GPIO (low-risk recommendation, not yet applied — awaiting approval per milestone rules)
- [ ] Grid-alignment cleanup pass on the 23 new `02_MCU` components (Minor, cosmetic only — see design review §11 for why it wasn't auto-fixed this pass)
- [ ] Confirm reserved-peripheral alternate-function pin choices (USART2/3, I2C1, SPI1, TIM1) against the STM32L462 datasheet AF table before those subsystems are wired in hardware
- [ ] Second-source qualification track for STM32L462RETx (Risk R-MCU-1, SWA-CST-004) — pin-compatible STM32L4 family alternates identified, not yet formally qualified

### Board Bring-up and Human Interface Subsystem
- [x] `03_HMI` schematic captured — 2026-07-31. Added Activity LED (PC8, new), User button (PC6, active-low, external pull-up + RC debounce, new), Reset button (parallel to existing NRST network, new), 3 new production test points (MCU_TX, MCU_RX, USER_BTN — 3.3V/GND/RESET reused existing TP2/TP3/TP_NRST1 rather than duplicating), and a schematic title block (previously unset) carrying board name/revision/date-code-placeholder/manufacturer-placeholder/project-ID. Power LED and MCU Status LED reviewed and reconfirmed correct, unchanged. PC6 and PC8 consumed from the `02_MCU` GPIO reservation/spare pool; PC7 remains reserved for a possible second future button. Board-ID silkscreen text and mounting holes are specified (§7 of the design doc) but **not physically placed** — both are `.kicad_pcb` layout-stage artifacts and no board outline or enclosure design exists yet (MCP tooling still has no footprint-placement capability, per the Tooling section below). Full writeup in [`Documentation/SWAFarmNodeV1_03HMI_DesignReview.md`](Documentation/SWAFarmNodeV1_03HMI_DesignReview.md).
- [ ] Implement 4x M3 non-plated mounting holes (corner pattern) and board-ID silkscreen text once a board outline/enclosure boss pattern exists (see design doc §7 for full specification)

### RS485 Communication Subsystem
- [x] `04_RS485` schematic captured — 2026-07-31. One THVD1450D transceiver (±18kV IEC ESD, fail-safe receiver — stock KiCad symbol) on the `02_MCU`-reserved USART3 (PB10/PB11) + PB2 combined DE/~RE, forming one shared RS485_A/RS485_B Modbus RTU bus. Five field connectors (4-pin Phoenix-style: A, B, VSYS_RAW_OUT, GND — hub/multidrop topology, NOT five independent interfaces) each with their own PTC fuse pair for per-cable fault isolation, converging into a single shared TVS-protected, DIP-switch-selectable terminated (120R) and biased (560R/560R) bus. 5 test points (A, B, TX, RX, combined DE/RE). `VSYS_RAW_OUT` reused verbatim from the original architecture doc's J4 net name and deliberately left unsourced (reserved for the future charger/VSYS milestone, confirmed via netlist trace it has zero drivers, not accidentally shorted to 3V3_RAIL). No changes made to `01_power`/`02_MCU`/`03_HMI` circuitry. Full writeup, bus topology diagram, and classified ERC results in [`Documentation/SWAFarmNodeV1_04RS485_DesignReview.md`](Documentation/SWAFarmNodeV1_04RS485_DesignReview.md).
- [x] **Critical symbol defect found and fixed during this milestone (not a previously-approved-circuitry issue) — 2026-07-31.** The stock KiCad `THVD1450D` symbol uses an `extends "MAX481E"` inheritance chain; the MCP placement tool's pin-copy logic only carried over 2 of 8 pins and left the `extends` reference unresolved in the schematic's own symbol cache, which would have broken the transceiver entirely (DE, DI, GND, A, B, VCC all physically missing). Fixed by rewriting the schematic's cached copy as a complete, self-contained 8-pin definition and correcting the placed instance; verified via `get_symbol_details` that all 8 pins resolve correctly. This produces one expected, documented `lib_symbol_mismatch` ERC warning (schematic's corrected copy vs. KiCad's own defective stock copy) — cosmetic only, the schematic's copy is authoritative for all ERC/netlist/manufacturing purposes.
- [ ] Wire `VSYS_RAW_OUT` to a real source once the charger/VSYS milestone (BQ25792) is captured
- [ ] Confirm hub/star RS485 topology against real field cable lengths and baud rate once firmware/field data exists (Risk R-RS485-1)
- [ ] Second-source qualification track for THVD1450D (Risk R-RS485-3, SWA-CST-004) — pin/footprint-compatible with the classic MAX485/SP3485/SN75176 family, not yet formally qualified
- [ ] Document DIP switch bias-pins-must-be-toggled-together requirement in firmware/field installation instructions (Risk R-RS485-4)

### LoRaWAN Communication Subsystem
- [x] `05_LoRaWAN` schematic captured — 2026-07-31. RAK3172 module, `RAK3172-xx-8-SM-xI` variant (verified from the KiCad library's own metadata as "RU864/IN865/EU868" — matches the India target region), confirming rather than changing the discrete-MCU-plus-certified-module architecture already decided in `02_MCU`. UART1 (AT-command interface) wired to the `02_MCU`-reserved USART2 (PA2/PA3, net names `LORA_TX`/`LORA_RX`), reset (PA0, `LORA_RESET_N`, pull-up + test point, same topology as the host MCU's own NRST), and a status GPIO (PA1, renamed `LORA_STATUS` — see note below). A real BOOT0 pin (not anticipated in the original `02_MCU` GPIO table) was found during datasheet/symbol verification and handled with the same pull-down + test-point pattern as the host MCU's own BOOT0. RF pin wired via a short unmatched 50Ω-target trace to a U.FL/IPEX connector (`Connector_Coaxial:U.FL_Hirose_U.FL-R-SMT-1_Vertical` footprint); external SMA antenna (via pigtail from the U.FL connector) selected over a PCB antenna for RF performance inside a sealed enclosure, long-range link budget, IP65 serviceability, and certification consistency — full rationale in the design doc. SWDIO/SWCLK exposed as unconnected test points only (rework contingency, not part of normal operation). No changes made to `01_power`–`04_RS485` circuitry. Full writeup in [`Documentation/SWAFarmNodeV1_05LoRaWAN_DesignReview.md`](Documentation/SWAFarmNodeV1_05LoRaWAN_DesignReview.md).
- [x] **Placement defect found and fixed during this milestone (not a previously-approved-circuitry issue) — 2026-07-31.** The U.FL antenna connector's shield/GND pin position was computed with a sign error (subtracted instead of added the local Y-offset per the standard pin-position formula), leaving the connector's RF shield floating — a real RF-performance defect (no ground reference for the coaxial shield), not just a cosmetic ERC item. Found via netlist trace (not obvious from ERC's default output), fixed by correcting the GND symbol's coordinate, independently re-verified.
- [x] **GPIO placeholder correction — 2026-07-31.** `02_MCU`'s GPIO table reserved PA1 as "LORA_BUSY," assuming a dedicated hardware busy signal. The verified RAK3172 pinout has no such pin (only general-purpose `PIN_Ax` GPIOs); renamed to `LORA_STATUS` on first actual implementation. This was a documentation-only placeholder, never wired, so not a change to approved circuitry.
- [x] **Gap 1 closed — 2026-07-31.** Cross-subsystem physical floor-plan/four-layer strategy document produced: [`Documentation/SWAFarmNodeV1_PCB_FloorPlan_and_LayerStrategy.md`](Documentation/SWAFarmNodeV1_PCB_FloorPlan_and_LayerStrategy.md). Reconciles all five captured subsystems plus the four not-yet-captured ones against the specified four-layer stack-up (L1 components/signals, L2 solid GND plane, L3 power distribution + low-speed signals, L4 components/signals): relative zone-adjacency floor plan, grounding strategy, RF/high-current/isolation/thermal constraints, connector/mounting placement rules, and explicit deferral of impedance geometry pending manufacturer stack-up selection. Planning artifact only — no `.kicad_pcb` file touched, no routing performed. Should be revisited at the start of each future milestone and again before PCB layout begins.
- [ ] **Gap 2 — still open, not yet acted on.** `04_RS485` did not consider galvanic isolation, which `.ai/agents/hardware_architect.md` explicitly recommends ("Consider isolated RS485 for production versions"). The new floor-plan document reserves spatial provision for an isolation moat (§7) without resolving the decision. Flagged for team review before production release — not unilaterally changed, per this session's "explain why + wait for approval" rule for previously-approved subsystems.
- [ ] Antenna gain / cable-loss link budget finalization once the enclosure and pigtail length are selected (Risk R-LORA-2)
- [ ] SMA bulkhead connector + U.FL pigtail cable — mechanical/enclosure-integration item, specified but not placed (design doc §5)
- [ ] Revisit powering the RAK3172 module through a dedicated load switch (the Power subsystem's originally-planned U4) instead of directly from `3V3_RAIL`, once that load switch is captured, for deeper sleep-current savings (Risk R-LORA-1)
- [ ] Second-source qualification track for RAK3172 (Risk R-LORA-3, SWA-CST-004)

### Industrial Relay Output Subsystem
- [x] `06_Relay` schematic captured — 2026-07-31. Five identical low-side N-MOSFET relay driver channels (`RELAY1`–`RELAY5`), each: MCU GPIO → 100Ω gate resistor → logic-level SOT-23 N-MOSFET → Finder 40.51 SPDT 12VDC/10A relay coil, 10kΩ gate pull-down for guaranteed default-OFF (including at MCU reset/boot), SMA-package flyback diode across the coil, and a status LED tapped off the control net at zero incremental GPIO cost. Consumes exactly the five GPIOs (`PB12`, `PB13`, `PB14`, `PB15`, `PA15`) `02_MCU` reserved for "Relay driver ×5" — no other subsystem's pins touched. Coil power (`RELAY_PWR`) is a locally-filtered branch (power ferrite bead + 22µF bulk cap) of the already-reserved `VSYS_RAW_OUT` net, reusing that net exactly as `04_RS485` did for sensor power, isolating relay switching noise from that shared trunk. Each channel has a 3-pin Phoenix MKDS screw-terminal field connector (COM/NO/NC, same footprint family as RS485's field connectors) with relay contacts deliberately **not** tied to board GND, preserving galvanic isolation between control and switched circuits. 5 per-channel control test points + 1 shared power test point + 1 shared ground test point. No changes made to `01_power`–`05_LoRaWAN` circuitry. Live ERC: 94 violations (47 errors / 47 warnings), down from the 99-violation pre-milestone baseline by exactly the 5 GPIO pins now wired — **zero new violations introduced**, independently confirmed via `generate_netlist`/`trace_netlist_connection` on every new node class. Full writeup, power calculations, and classified ERC results in [`Documentation/SWAFarmNodeV1_06Relay_DesignReview.md`](Documentation/SWAFarmNodeV1_06Relay_DesignReview.md).
- [ ] `VSYS_RAW_OUT` now has two disclosed consumers (RS485 sensor power + ~385mA worst-case relay coil draw) with no real source yet — must be accounted for when the battery-charger/VSYS milestone is sized (Risk R-RELAY-1; combined total updated to ≈885mA worst-case in `07_FieldIO`, see below)
- [ ] Confirm Finder 40.51.9.012 coil current/power against a locked datasheet before BOM release (Risk R-RELAY-2)
- [ ] Second-source qualification track for the Finder 40.51 family (Risk R-RELAY-3, SWA-CST-004)
- [ ] Relay-bank thermal assessment against real enclosure airflow at PCB layout stage (Risk R-RELAY-4)
- [ ] Contact-side creepage/clearance analysis if any channel is expected to switch mains-referenced loads (Risk R-RELAY-5)

### Field I/O & Sensor Interface Subsystem
- [x] `07_FieldIO` schematic captured — 2026-07-31. Reviewed the five RS485 sensor field connectors already captured in `04_RS485` (`J_RS485_SENSOR1`–`5`, one shared bus, one transceiver) and confirmed they already satisfy the "five connectors, same bus, RS485 A/B + Sensor Power + Ground" requirement — **no new or duplicate connectors created**. Closed the one real gap found: the Sensor Power pin had zero protection (directly on the shared, unfused `VSYS_RAW_OUT` trunk). Added one PTC fuse per connector (`F_RS485_PWR1`–`5`, same 0.2A-hold class already qualified for RS485 A/B in `04_RS485`, mirroring that exact per-drop-fuse/shared-TVS-once pattern) plus one shared unidirectional TVS (`D_SENSOR_PWR_TVS1`, `SMBJ18A` class) upstream of all five, providing surge/ESD protection and crude reverse-polarity clamping via the same passive clamp-and-fuse approach standard for low-power field power outputs. **Missing-requirement gate hit and resolved before implementation**: no `.ai/knowledge` document specifies a sensor current draw or count; asked the user directly rather than guessing, approved planning figure is 100mA/sensor, 500mA total worst-case — used to size the fuse, documented as an assumption pending real sensor selection (Risk R-FIO-1). Combined disclosed `VSYS_RAW_OUT` worst-case demand from RS485 sensor power (this milestone) + relay coils (`06_Relay`) is now **≈885mA** — the number the future charger/VSYS milestone must be sized against. Mechanical clearance reservations for field-connector cable approach, tool access, strain relief, and labeling recorded for both the RS485 sensor and relay connector families (floor-plan doc updated). No changes to RS485 topology, GPIO allocation, or any other previously-approved circuitry. Live ERC: 94 violations (47/47), identical to the pre-milestone baseline — **zero new violations**, independently confirmed via netlist trace. Full writeup in [`Documentation/SWAFarmNodeV1_07FieldIO_DesignReview.md`](Documentation/SWAFarmNodeV1_07FieldIO_DesignReview.md).
- [ ] Confirm the 100mA/sensor planning assumption against real selected sensor models before release (Risk R-FIO-1)
- [ ] Size the battery-charger/VSYS milestone against the combined ≈885mA worst-case `VSYS_RAW_OUT` demand (sensors + relays) (Risk R-FIO-2)
- [ ] Verify PTC fuse resistance/voltage-drop against a locked datasheet once real cable-run lengths are known (Risk R-FIO-3)
- [ ] Validate field-connector mechanical clearance reservations against a real enclosure/cable-gland selection (Risk R-FIO-4)

### Other Subsystems (not started)
- [ ] Sensor front-end (5+ channels)
- [ ] Enclosure / mechanical design

## Tooling
- [x] KiCad MCP server configured — 2026-07-30. Seeed-Studio/kicad-mcp-server installed into KiCad 10.0's bundled Python (`C:\Program Files\KiCad\10.0\bin\python.exe`), registered with Claude Code (`claude mcp add kicad -s user`) at `C:\Users\prave\mcp-servers\kicad-mcp-server`, scoped to this project via `KICAD_PROJECT_PATHS`. Enables AI-assisted schematic/PCB analysis, netlist tracing, and ERC/DRC directly against `pcbnew` — unblocks the schematic capture and ERC steps above. Editing tools are experimental (manual S-expression manipulation); schematic capture itself should still be done in the KiCad GUI per this file's existing guidance.
- [x] KiCad MCP editing-tool defects found and worked around — 2026-07-30, during `01_power` capture:
  - `add_component_from_library`/`create_kicad_project` stamp a fictional schema `(version 20260306)`/`(generator_version "10.0")` that the installed KiCad 10.0.5 `kicad-cli` refuses to load ("Failed to load schematic" with no further detail). Fix: use a real, backward-compatible schema (`20250114`/`"9.0"`) — confirmed working. Both `SWAFarmNodeV1.kicad_sch` and `.kicad_pcb` had this bad stamp pre-existing from an earlier session and were corrected as part of this milestone.
  - `add_component_from_library` never writes the `(instances (project "..." (path "/<sheet-uuid>" (reference "...") (unit N))))` sub-block that modern KiCad schematic format requires per symbol instance. Without it, `kicad-cli sch erc` reports every pin on that symbol as "not connected" even when wires/labels are geometrically exact — this was the dominant source of false ERC errors this session, not bad coordinates. Fix: inject the block manually after placement.
  - `add_wire` silently accepts more than 2 points and writes an invalid multi-point `(wire (pts ...))` element that fails to load entirely. Always call it with exactly 2 points per segment; use multiple calls for bends.
  - Pin absolute position = `(symbol_origin_x + pin_local_x, symbol_origin_y − pin_local_y)` — the Y offset from a symbol's own library definition is negated when placed on a sheet (library Y-up vs. sheet Y-down). Getting this backwards silently swaps which physical pin a wire lands on (caught via `kicad-cli sch export netlist`, not ERC).
  - `setup_pcb_layout` fills the new `.kicad_pcb` with an unrelated placeholder footprint/nets and an invalid `title_block` field (`ki_producers`) that also fails to load — both were stripped out; only the board outline was kept.
  - No footprint-placement or routing tool exists in this MCP server's toolset — DRC is only meaningful after that's done in the KiCad GUI.
- [x] Schematic-file structure gotcha found — 2026-07-31, during `06_Relay` bulk capture (66 new components, generated by script and spliced directly into the `.kicad_sch` rather than placed one-by-one via MCP, since per-component tool calls were impractical at this scale): `(lib_symbols ...)` closes far earlier in this file than the top-level `(sheet_instances ...)` block appears — a large run of legacy verbose-format placed symbols (from early `01_power`/`02_MCU` capture) sits between the two. Splicing new library-template content in based on adjacency to `(sheet_instances ...)` lands inside the placed-symbol region instead, and even after locating the real closing paren (via a string-aware, quote-respecting paren-depth counter — a naive counter can be fooled by parentheses inside property text like Description/Datasheet fields), new content must be inserted **before** that closing paren, not after — inserting after silently places new symbol templates outside `lib_symbols` entirely. Both mistakes produced files that parsed fine under the MCP server's own lenient parser (`get_schematic_info` reported the correct component count either way) but that real `kicad-cli sch erc` flatly refused to load ("Failed to load schematic", no line or reason given). Fix/process: locate `lib_symbols`' true end with a string-aware paren counter, insert new template blocks immediately before that closing paren, and always verify with real `kicad-cli` — never trust the MCP parser alone — before trusting a bulk splice.
- [x] New `add_component_from_library` defect found — 2026-07-31, during `04_RS485` capture: for stock symbols that use KiCad's `extends` inheritance (e.g. `Interface_UART:THVD1450D` extends `MAX481E` extends `LTC2850xS8`), the tool only copies the named symbol's own (often pin-less) block into the schematic's `lib_symbols` cache — it does **not** walk the inheritance chain to pull in the base symbol(s) that actually define the pins/graphics, and it does not warn that the resulting definition is incomplete. The placed instance also only gets as many `(pin ...)` entries as the tool's (wrong) pin count, not the true pin count. Symptom: `add_component_from_library`'s own success message reports an implausibly low pin count (e.g. "2 pins detected" for an 8-pin IC) — treat that as a red flag requiring manual verification via `get_symbol_details()` before wiring. Fix: replace the schematic's cached `lib_symbols` entry with a self-contained definition (no `extends`) carrying the real pins/graphics copied from the ultimate base symbol, and correct the placed instance's pin list to match. Produces one expected, harmless `lib_symbol_mismatch` ERC warning versus KiCad's own stock copy.
- [x] Grid-alignment process error found and fixed — 2026-07-31, during `08_BatterySolar` bulk capture: hand-picked "round number" origin coordinates (e.g. `(200.00, 900.00)`) for new-component placement, forgetting that convenient decimal millimeter values are very often *not* exact multiples of 1.27mm (the KiCad connection grid) — the same class of mistake already documented from `02_MCU`. First ERC pass showed 70 new `endpoint_off_grid` warnings as a result. Fixed with a targeted regex pass snapping only origin-variable assignments (identified by naming convention, e.g. `*_O = (x, y)`) to the nearest 1.27mm multiple — deliberately excluding any coordinate copied from a real, already-existing component pin (e.g. the verified MCU pin used for the battery-voltage ADC connection), since blind-snapping those would silently disconnect the label from the actual pin it needed to reach. Lesson: when bulk-generating placements programmatically, choose grid-valid origins (multiples of 1.27mm, ideally 2.54mm) at the point of picking coordinates, not as a post-hoc fix — and never apply an automated snap/transform across a script without first checking whether any of its coordinates are copied from real, external, must-stay-exact positions.

## Notes
- Milestones are appended here per CLAUDE.md instruction; this file tracks completion status only. Design rationale lives in `Documentation/`, normative engineering rules live in `.ai/knowledge/`.
